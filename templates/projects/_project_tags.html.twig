<!-- MADE BY MACIEJ MORYŃ -->

{% if not post.tags.empty %}
    <p class="post-tags">
        {% for tag in post.tags %}
            <a href="{{ path('projects_index', {'tag': tag.name == app.request.query.get('tag') ? null : tag.name}) }}"
               class="badge badge-pill badge-{{ tag.name == app.request.query.get('tag') ? 'success' : 'primary' }}"
            >
                <i class="fa fa-tag"></i> {{ tag.name }}
            </a>
        {% endfor %}
    </p>
{% endif %}

